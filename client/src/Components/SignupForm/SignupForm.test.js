it("prevents sign up if password and confirm password don't match", async () => {});

it("hides password input with *", async () => {});

it("triggers the trigger function when user clicks on 'here'", async () => {});

it("fires the POST request when everything is entered correctly", async () => {});

it("displays alert and prevents submission if email has no '@'", async () => {});
it("creates the correct object structure for the POST request", async () => {});
it("displays an alert if response to POST request says that user already exists", async () => {});
